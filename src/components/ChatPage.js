import React from "react";
import './ChatPage.css'



function ChatPage(props) {
    return (
      <div id="chatpage">
           <div className="page">
           <div className="card" >
      
  <div className="card-body">
    <h5 className="card-title">Cd Command</h5>
    <p className="card-text">The cd command is one of the most used Linux commands. The purpose of this command is to change the directory. This cd command, popularly called chdir (change directory), is a code administrative command used in several versions of Windows that can modify the home directory. It could be applied in routine programs and terminal scripts. In other words, it enables to switch between file lists. For example, if you needed to shift from the home directory to the myfile directory, you would enter the following command:</p>
    <a href="#" class="btn btn-primary">Learn More</a>
  </div>
</div>
           </div>
      </div>
    );
  }
  
  export default ChatPage;